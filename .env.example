# TradePilot — Environment Variables
# Copy this file to .env and fill in your API keys.
# Rename: cp .env.example .env

# ─── FRED API (Federal Reserve Economic Data) ─────────────────────────────────
# Free API key: https://fred.stlouisfed.org/docs/api/api_key.html
# Used for: GDP, CPI, NFP, Unemployment, PCE, Retail Sales, JOLTS, Fed Funds Rate
# US Treasury yields: DGS2, DGS5, DGS10, DGS30
# Without key: US macro data falls back to mock (other economies always use mock)
VITE_FRED_API_KEY=your_fred_api_key_here

# ─── TwelveData (Market Price Data) ───────────────────────────────────────────
# Free API key: https://twelvedata.com/pricing (800 calls/day, 8/min)
# Used for: Real OHLC price data → SMA20/50/100/200, RSI14, trend detection
# Covers: FX pairs, XAU/USD, XAG/USD, BTC/USD, SPY (S&P 500 proxy), QQQ (Nasdaq proxy)
# Without key: Technical indicators fall back to mock (deterministic seed-based)
VITE_TWELVE_DATA_API_KEY=your_twelvedata_api_key_here

# ─── Notes ────────────────────────────────────────────────────────────────────
# COT Data (CFTC): No key needed — public SODA API with CORS support
# Sentiment (Myfxbook): No key needed — public community outlook endpoint
# Supabase: Keys are hardcoded in config.ts (project: wegeehbxrsokuknftkwc)
